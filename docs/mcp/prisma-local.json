{
  "mcpServers": {
    "prisma-local": {
      "command": "pnpm",
      "args": ["prisma", "mcp"],
      "cwd": "/absolute/path/to/purrmission",
      "env": {
        "DATABASE_URL": "file:./data/dev.db"
      }
    }
  }
}
