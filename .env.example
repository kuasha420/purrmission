# Prisma SQLite database for Purrmission
DATABASE_URL="file:./prisma/purrmission.db"

# Encryption key for encrypting sensitive data at rest (TOTP secrets, resource fields)
# Must be a 32-byte hex string (64 hexadecimal characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=""
