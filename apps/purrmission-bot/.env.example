# Discord Bot Configuration
DISCORD_BOT_TOKEN=your-bot-token-here
DISCORD_CLIENT_ID=your-client-id-here
DISCORD_GUILD_ID=your-guild-id-for-dev

# HTTP Server Configuration
APP_PORT=3000

# Database (Prisma SQLite, reused from root if desired)
DATABASE_URL=file:../../prisma/purrmission.db

# (future) Encryption for TOTP secrets
# TOTP_ENCRYPTION_KEY=change-me-to-a-32-byte-secret

# Encryption key for resource field values (required for /purrmission resource commands)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ENCRYPTION_KEY=your-64-char-hex-key-here
